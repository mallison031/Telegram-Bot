python-telegram-bot[webhooks,job-queue]>=21.0
google-genai>=1.0.0
python-dotenv>=1.0.0
pydantic>=2.0
httpx>=0.27
pytz
# Guarantees zoneinfo can resolve TIMEZONE on slim images that ship no system
# tz database — without it the morning/night texts silently fall back to UTC.
tzdata
